'use strict';
import fisheryConst = require('./fisheryConst');
import { pinus } from 'pinus';
import { GameNidEnum } from '../../../common/constant/game/GameNidEnum';

//重置开奖记录
export const resetHistory = async function () {
    // const rooms = await RoomManager.getRoomsOfGame(GameNidEnum.fishery, false);
    // rooms.forEach(async (room) => {
    //     room['fisheryHistory'] = [];
        // RoomManager.updateOneRoomFromCluster(pinus.app.getServerId(), room, ['fisheryHistory']);
    // });
}