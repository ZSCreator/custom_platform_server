import benzRoom from "../benzRoom";

/**
 * 构建记录需要结果
 * @param lotteryResults 开奖结果
 */
export function buildRecordResult(roomInfo: benzRoom) {
    // 前缀
    let prefix = roomInfo.lotterys;

    return prefix;
}